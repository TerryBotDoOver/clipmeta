import { GeneratorLandingPage, metadataForGeneratorPage } from "@/components/seo/generatorPages";

export const metadata = metadataForGeneratorPage("shutterstock-video-keywording");

export default function ShutterstockVideoKeywordingPage() {
  return <GeneratorLandingPage slug="shutterstock-video-keywording" />;
}
